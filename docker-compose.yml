services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      ENVIRONMENT: docker
      CHROMA_DIR: /app/chroma_db
      STORAGE_DIR: /app/storage
    volumes:
      - ./storage:/app/storage
      - ./chroma_db:/app/chroma_db
